var amount = 99.99;

amount = amount * 2;

console.log( amount ); // 199.98

// convert 'amount' to a string, and
// add "$" on the begining
amount = "$" + String( amount );

console.log( amount ); // "$199.98"
